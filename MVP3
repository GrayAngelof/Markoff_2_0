## Техническое Задание: Клиентская часть Markoff
## MVP 3

### Введение и общая концепция

Данный документ описывает архитектуру клиентского приложения Markoff — десктопного инструмента для управления недвижимостью, работающего в связке с FastAPI бэкендом и PostgreSQL. Клиент реализован на PySide6 и представляет собой событийно-ориентированную систему с чётким разделением на слои.

**Ключевая идея:** Приложение строится как реактивная система, где все компоненты общаются через центральную шину событий (Event Bus), а данные хранятся в оптимизированном графе сущностей (Entity Graph). UI полностью отделён от бизнес-логики и данных.

---

### 1. Общая архитектура клиента

Система состоит из пяти основных слоёв, каждый со строго определённой зоной ответственности:

```
┌─────────────────────────────────────────────────────────────┐
│                         UI Layer                             │
│  (Widgets: TreeView, DetailsPanel, MainWindow)              │
└─────────────────────────────┬───────────────────────────────┘
                              │ генерирует команды
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Event Bus (core/)                        │
│  ┌─────────────┐    ┌─────────────┐                         │
│  │  UI Events  │    │ System Events│                         │
│  │  (команды)  │    │   (факты)   │                         │
│  └─────────────┘    └─────────────┘                         │
└─────────────────────────────┬───────────────────────────────┘
                ┌──────────────┼──────────────┐
                ▼              ▼              ▼
┌────────────────────────┐ ┌────────────────────────┐ ┌────────────────────────┐
│    Services Layer      │ │   Projections Layer    │ │   Data Layer           │
│  (бизнес-логика)       │ │ (построители UI-форм)  │ │   (Entity Graph)       │
│ • DataService          │ │ • TreeProjection       │ │ • Хранилище сущностей  │
│ • ConnectionService    │ │ • (другие проекции)    │ │ • Индексы связей       │
└────────────────────────┘ └────────────────────────┘ └────────────────────────┘
```

#### 1.1 Принципы взаимодействия слоёв

1. **UI Layer** генерирует только команды (`event_bus.ui.*`) и подписывается на факты (`event_bus.sys.*`) для обновления своего состояния.
2. **Services Layer** подписывается на команды, выполняет бизнес-логику и генерирует факты о результатах.
3. **Data Layer (Entity Graph)** хранит данные в плоском виде и не знает о событиях — это просто структура данных.
4. **Projections Layer** подписывается на факты об изменениях данных и строит из плоского хранилища структуры, удобные для UI (деревья, списки и т.д.).

---

### 2. Детальное описание компонентов

#### 2.1 Core Layer (ядро)

**2.1.1 Event Bus (core/event_bus.py)**

Центральный элемент системы — единая шина событий с двумя логическими каналами.

```python
# Концептуальная структура
EventBus
  ├── ui: UIEventsChannel     # Команды пользователя
  └── sys: SystemEventsChannel # Факты о состоянии системы
```

**UI Events (команды):**
- `node_selected(type, id)` — пользователь выбрал узел
- `node_expanded(type, id)` — пользователь раскрыл узел
- `node_collapsed(type, id)` — пользователь свернул узел
- `refresh_requested(mode)` — запрос обновления (current/visible/full)
- `tab_changed(index)` — переключение вкладки

**System Events (факты):**
- `data_loading(type, id)` — началась загрузка данных
- `data_loaded(type, id, data)` — данные успешно загружены
- `data_error(type, id, error)` — ошибка загрузки
- `connection_changed(is_online)` — изменилось состояние соединения
- `cache_updated(entity_type, count)` — обновлён кэш

#### 2.2 Data Layer (хранение данных)

**2.2.1 Entity Graph (data/entity_graph.py)**

Хранит все сущности в плоском виде с индексами для быстрого доступа. Не содержит логики, только данные и связи.

**Структура хранения:**
- `stores[entity_type][id] = entity` — словарь всех сущностей по типам
- `indices[entity_type][parent_id] = [child_ids]` — прямые индексы (родитель → дети)
- `parents[entity_type][child_id] = parent_id` — обратные индексы (ребёнок → родитель)

**Поддерживаемые типы сущностей:**
- `complex` — комплексы
- `building` — корпуса
- `floor` — этажи
- `room` — помещения

**Операции:**
- `update(entity_type, id, data, parent_id)` — добавить или обновить сущность
- `get_entity(entity_type, id)` — получить сущность по ID
- `get_children(entity_type, parent_id)` — получить ID всех детей родителя
- `get_parent(entity_type, child_id)` — получить ID родителя
- `get_all_ids(entity_type)` — получить все ID данного типа

#### 2.3 Services Layer (бизнес-логика)

**2.3.1 DataService (services/data_service.py)**

Центральный сервис для работы с данными. Подписывается на UI-команды, взаимодействует с API и EntityGraph.

**Обязанности:**
- Проверка наличия данных в EntityGraph
- Загрузка с API при необходимости
- Обновление EntityGraph
- Генерация системных событий о результате

**Поток работы:**
1. Получает команду `ui.node_selected` или `ui.node_expanded`
2. Проверяет наличие данных в графе
3. Если данных нет — запрашивает через API
4. Обновляет граф
5. Генерирует `sys.data_loaded`

**2.3.2 ConnectionService (services/connection_service.py)**

Мониторит соединение с бэкендом через `/health` endpoint.

**Обязанности:**
- Периодическая проверка соединения (каждые 30 секунд)
- Генерация `sys.connection_changed` при изменении статуса
- Реагирование на ручные проверки (через `ui.connection_check_requested`)

#### 2.4 Projections Layer (построение представлений)

**2.4.1 BaseProjection (projections/base_projection.py)**

Базовый класс для всех проекций с поддержкой debounce.

**Механизм debounce:**
- При получении сигнала об изменении данных запускается таймер
- Если в течение 50 мс приходят новые сигналы, таймер перезапускается
- По истечении таймера выполняется перестроение

**2.4.2 TreeProjection (projections/tree_projection.py)**

Строит из плоских данных EntityGraph иерархическое дерево для TreeModel.

**Вход:** плоские данные из графа
**Выход:** структура вида `[Complex {children: [Building {children: [Floor {children: [Room]}]}]}]`

**Правила построения:**
- Берёт все комплексы
- Для каждого комплекса находит корпуса через индекс
- Для каждого корпуса находит этажи
- Для каждого этажа находит помещения
- Никакой бизнес-логики, только группировка

**Особенности:**
- Перестраивается только при реальных изменениях данных (через debounce)
- Результат кэшируется до следующего изменения

#### 2.5 UI Layer (презентация)

**2.5.1 TreeModel (ui/tree_model/)**

Модель для QTreeView. Получает уже готовое дерево от TreeProjection и просто отображает его.

**Обязанности:**
- Отображение данных в соответствии с ролями Qt
- Обработка запросов на раскрытие узлов (генерация `ui.node_expanded`)
- Обработка выбора узлов (генерация `ui.node_selected`)
- Кастомизация внешнего вида (цвета, шрифты)

**Важно:** TreeModel больше не управляет данными, только их отображением.

**2.5.2 TreeView (ui/tree/)**

Визуальный компонент дерева. Минимальная логика:
- Проксирование событий мыши в команды EventBus
- Отображение данных из TreeModel

**2.5.3 DetailsPanel (ui/details/)**

Панель детальной информации. Подписывается на `sys.data_loaded` и отображает данные.

**Поведение:**
- При `sys.data_loaded` обновляет содержимое
- При смене вкладки генерирует `ui.tab_changed`
- Не знает, откуда пришли данные

**2.5.4 MainWindow (ui/main_window/)**

Композиционный корень приложения. Собирает все компоненты вместе и настраивает подписки.

**Обязанности:**
- Создание всех UI-компонентов
- Размещение их в окне (splitter, toolbar, status bar)
- Настройка горячих клавиш
- Инициализация сервисов и проекций
- Подписка на события для обновления статус-бара

**Важно:** Никакой бизнес-логики, только композиция.

---

### 3. Полные потоки данных

#### 3.1 Загрузка приложения (инициализация)

```
1. MainWindow создаёт компоненты
2. MainWindow инициализирует сервисы и проекции
3. DataService генерирует ui.refresh_requested('full')
4. DataService загружает комплексы через API
5. DataService обновляет EntityGraph
6. DataService генерирует sys.data_loaded('complex', ...)
7. TreeProjection (подписан) получает событие, запускает debounce
8. Через 50 мс TreeProjection перестраивает дерево
9. TreeProjection обновляет TreeModel
10. TreeView отображает комплексы
```

#### 3.2 Пользователь раскрывает комплекс

```
1. Пользователь кликает на "+" у комплекса
2. TreeView генерирует ui.node_expanded('complex', 1)
3. DataService получает команду
4. DataService проверяет наличие корпусов в EntityGraph
5. Если нет — запрашивает через API
6. DataService обновляет граф (добавляет корпуса)
7. DataService генерирует sys.data_loaded('building', ...)
8. TreeProjection перестраивает дерево (debounce)
9. TreeModel обновляется, TreeView показывает корпуса
```

#### 3.3 Пользователь выбирает помещение

```
1. Пользователь кликает на помещение
2. TreeView генерирует ui.node_selected('room', 21)
3. DataService получает команду
4. DataService проверяет наличие детальных данных в графе
5. Если нет — запрашивает через API
6. DataService обновляет граф
7. DataService генерирует sys.data_loaded('room', 21, detailed_data)
8. DetailsPanel (подписан) получает событие
9. DetailsPanel отображает детальную информацию
```

#### 3.4 Пользователь нажимает F5 (обновить текущий узел)

```
1. MainWindow перехватывает горячую клавишу
2. MainWindow генерирует ui.refresh_requested('current')
3. DataService получает команду
4. DataService определяет текущий выбранный узел
5. DataService инвалидирует кэш для этого узла
6. DataService загружает свежие данные через API
7. DataService обновляет граф
8. DataService генерирует sys.data_loaded
9. TreeProjection перестраивает дерево
10. TreeModel обновляется
```

#### 3.5 Потеря соединения с бэкендом

```
1. ConnectionService периодически проверяет /health
2. Запрос не проходит (таймаут/ошибка)
3. ConnectionService генерирует sys.connection_changed(False)
4. StatusBar (подписан) меняет иконку на "офлайн"
5. Toolbar (подписан) блокирует кнопки обновления
6. DataService перестаёт делать запросы (или ставит в очередь)
7. При восстановлении соединения генерируется sys.connection_changed(True)
8. UI возвращается в нормальный режим
```

---

### 4. Правила архитектуры (железобетонные)

#### 4.1 Правило команд и фактов
- **UI генерирует только команды** (`event_bus.ui.*`) — запросы на действие
- **Сервисы генерируют только факты** (`event_bus.sys.*`) — сообщения о свершившихся событиях
- Никто не генерирует "чужие" события

#### 4.2 Правило тупого хранилища
- **EntityGraph не содержит логики** — только методы get/set
- Всё, что сложнее "положить/достать" — в сервисы
- Граф не знает о событиях

#### 4.3 Правило тупой проекции
- **Проекция только строит структуру**, не анализирует
- Нет условий "если пользователь админ", "если комната архивная"
- Проекция = группировка, ничего больше

#### 4.4 Правило debounce
- Любая операция, которая может вызываться >10 раз в секунду, ДОЛЖНА иметь debounce
- Особенно это касается перестроения дерева и обновления UI
- Debounce через QTimer.singleShot с таймаутом 50-100 мс

#### 4.5 Правило контроллеров
- Контроллер создаётся ТОЛЬКО если есть логика, не укладывающаяся в простую подписку
- Простое проксирование события на метод виджета — не повод для контроллера
- Виджеты могут подписываться на события напрямую

#### 4.6 Правило иммутабельности событий
- События не изменяются после создания
- Все данные в событиях — копии или иммутабельные структуры
- Никто не может изменить событие после отправки

---

### 5. Обработка ошибок

#### 5.1 Ошибки API
- DataService перехватывает все исключения при запросах
- Генерирует `sys.data_error` с информацией об ошибке
- StatusBar отображает сообщение об ошибке
- EntityGraph НЕ обновляется

#### 5.2 Ошибки соединения
- ConnectionService отслеживает потерю связи
- Все сервисы прекращают попытки запросов (или ставят в очередь)
- UI блокирует действия, требующие соединения
- При восстановлении генерируется событие, UI разблокируется

#### 5.3 Ошибки в проекциях
- Проекции не должны падать — любая ошибка логируется, но не ломает UI
- При ошибке проекция возвращает последнее корректное состояние
- Таймер debounce сбрасывается, попытка повторится через некоторое время

---

### 6. Производительность и оптимизация

#### 6.1 Индексы в EntityGraph
- Доступ к любой сущности по ID — O(1)
- Получение всех детей родителя — O(1) (готовый список)
- Построение дерева — O(N) только при изменениях

#### 6.2 Debounce для проекций
- 50 мс таймаут для накопления изменений
- При массовых обновлениях (загрузка этажа с 50 комнатами) — 1 перестроение
- При быстрых кликах пользователя — не лагает

#### 6.3 Кэширование проекций
- TreeProjection хранит результат последнего построения
- Если данные не менялись — отдаёт кэш
- События `cache_updated` сбрасывают кэш

#### 6.4 Ленивая загрузка
- Данные загружаются только при необходимости (раскрытии узла)
- EntityGraph заполняется постепенно
- Нет предзагрузки всего дерева

---

### 7. Тестирование

#### 7.1 Модульное тестирование
- **EntityGraph** — тесты добавления, обновления, индексов
- **EventBus** — тесты подписки/отписки, доставки событий
- **Projections** — тесты построения структур из данных

#### 7.2 Интеграционное тестирование
- **DataService + EntityGraph** — проверка потока "запрос → обновление"
- **UI события → реакция сервисов** — проверка подписок

#### 7.3 Сквозное тестирование
- **Пользовательские сценарии** через симуляцию событий
- Проверка, что UI обновляется при получении фактов

#### 7.4 Нагрузочное тестирование
- Симуляция 100+ событий в секунду
- Проверка работы debounce
- Отсутствие утечек памяти при длительной работе

---

### 8. Критерии готовности

1. **Event Bus** реализован как единый синглтон с двумя каналами
2. **Entity Graph** реализован с индексами и обратными связями
3. **DataService** корректно обрабатывает все UI-команды
4. **TreeProjection** с debounce правильно строит дерево
5. **TreeModel** получает данные от проекции, а не управляет ими
6. **UI компоненты** не содержат бизнес-логики
7. **Все коммуникации** идут через события
8. **Логирование** событий включено в debug-режиме
9. **Тесты** покрывают ключевые сценарии
10. **Производительность** не хуже, чем до рефакторинга

---

### 9. Заключение

Предложенная архитектура превращает клиентское приложение из набора связанных виджетов в **событийно-ориентированную систему с чёткими границами ответственности**. 

**Ключевые достижения:**
- UI полностью отделён от данных и логики
- Данные хранятся в оптимизированной для чтения структуре
- Проекции обеспечивают быструю адаптацию данных под нужды UI
- Event Bus гарантирует слабую связанность компонентов
- Система готова к масштабированию и добавлению новых фич

Это архитектура уровня промышленных приложений, способная развиваться годами без необходимости кардинальных переделок.


## Полная структура папок MVP 3

```
client/
├── src/
│   ├── core/                          # Ядро системы
│   │   ├── __init__.py
│   │   ├── event_bus.py                # Центральная шина событий
│   │   │   ├── Event (базовый класс)
│   │   │   ├── EventBus (синглтон)
│   │   │   ├── UIEventsChannel (команды пользователя)
│   │   │   └── SystemEventsChannel (факты системы)
│   │   ├── constants.py                 # Общие константы (типы событий, режимы)
│   │   └── app_context.py                # Контекст приложения (доступ к шине, графу)
│   │
│   ├── data/                           # Слой хранения данных
│   │   ├── __init__.py
│   │   ├── entity_graph.py              # Граф сущностей с индексами
│   │   │   ├── EntityType (enum)
│   │   │   ├── stores[type][id]         # Хранилище сущностей
│   │   │   ├── indices[type][parent_id] # Прямые индексы (родитель → дети)
│   │   │   ├── parents[type][child_id]  # Обратные индексы (ребёнок → родитель)
│   │   │   └── методы: update(), get_entity(), get_children(), get_parent()
│   │   └── cache.py                      # (переиспользованный) Кэш HTTP-ответов
│   │
│   ├── services/                        # Слой бизнес-логики
│   │   ├── __init__.py
│   │   ├── api_client.py                 # (переиспользованный) HTTP-клиент
│   │   │   ├── get_complexes()
│   │   │   ├── get_buildings()
│   │   │   ├── get_floors()
│   │   │   ├── get_rooms()
│   │   │   └── get_detail() методы
│   │   ├── data_service.py                # Центральный сервис данных
│   │   │   ├── подписка на ui.node_*
│   │   │   ├── подписка на ui.refresh_*
│   │   │   ├── проверка графа
│   │   │   ├── загрузка через API
│   │   │   ├── обновление графа
│   │   │   └── генерация sys.data_*
│   │   └── connection_service.py          # Мониторинг соединения
│   │       ├── периодическая проверка /health
│   │       ├── генерация sys.connection_changed
│   │       └── реакция на ручные проверки
│   │
│   ├── projections/                      # Слой построения представлений
│   │   ├── __init__.py
│   │   ├── base_projection.py             # Базовый класс с debounce
│   │   │   ├── _debounce_timer
│   │   │   ├── _cached_result
│   │   │   ├── _debounced_rebuild()
│   │   │   └── get() (возвращает кэш или строит)
│   │   └── tree_projection.py             # Строит дерево для TreeModel
│   │       ├── подписка на sys.data_loaded
│   │       ├── построение иерархии из графа
│   │       └── структура [Complex {children: [Building {children: [Floor ...]}]}]
│   │
│   ├── ui/                               # Презентационный слой
│   │   ├── __init__.py
│   │   ├── widgets/                       # Переиспользуемые виджеты
│   │   │   ├── __init__.py
│   │   │   ├── refresh_menu.py             # (переиспользованный) Меню обновления
│   │   │   └── status_badge.py              # Виджет статуса с цветом
│   │   │
│   │   ├── tree/                           # Компоненты дерева
│   │   │   ├── __init__.py
│   │   │   ├── tree_model.py                # Модель для QTreeView
│   │   │   │   ├── update_tree(new_tree)    # единственный метод изменения
│   │   │   │   ├── data() (роли Qt)
│   │   │   │   ├── flags()
│   │   │   │   ├── index()/parent()
│   │   │   │   └── генерация ui.node_* при действиях пользователя
│   │   │   └── tree_view.py                 # Визуальный компонент
│   │   │       ├── подключение модели
│   │   │       ├── проксирование событий мыши
│   │   │       └── стили (из старого base_tree.py)
│   │   │
│   │   ├── details/                         # Панель детальной информации
│   │   │   ├── __init__.py
│   │   │   ├── header_widget.py              # (переиспользованный) Шапка с иерархией
│   │   │   ├── info_grid.py                   # (переиспользованный) Сетка полей
│   │   │   ├── placeholder.py                  # (переиспользованный) Заглушка
│   │   │   ├── tabs.py                         # (переиспользованный) Вкладки
│   │   │   ├── field_manager.py                 # (переиспользованный) Форматирование полей
│   │   │   ├── display_handlers.py              # (переиспользованный) Отображение разных типов
│   │   │   └── details_panel.py                 # Основная панель
│   │   │       ├── подписка на sys.data_loaded
│   │   │       ├── обновление содержимого
│   │   │       └── генерация ui.tab_changed
│   │   │
│   │   └── main_window/                       # Главное окно (композиционный корень)
│   │       ├── __init__.py
│   │       ├── components/                      # Компоненты окна
│   │       │   ├── __init__.py
│   │       │   ├── central_widget.py            # (переиспользованный) Разделитель
│   │       │   ├── toolbar.py                    # (переиспользованный) Панель инструментов
│   │       │   └── status_bar.py                  # (переиспользованная) Строка статуса
│   │       ├── controllers/                      # Контроллеры (только где нужно)
│   │       │   ├── __init__.py
│   │       │   └── (пусто - вся логика в сервисах)
│   │       ├── shortcuts.py                      # (переиспользованные) Горячие клавиши
│   │       └── main_window.py                    # Основной класс
│   │           ├── инициализация в порядке:
│   │           │   1. Event Bus
│   │           │   2. Entity Graph
│   │           │   3. API Client
│   │           │   4. Services
│   │           │   5. Projections
│   │           │   6. UI компоненты
│   │           ├── настройка подписок
│   │           └── компоновка UI
│   │
│   ├── models/                            # Модели данных (переиспользованные)
│   │   ├── __init__.py
│   │   ├── complex.py
│   │   ├── building.py
│   │   ├── floor.py
│   │   └── room.py
│   │
│   └── utils/                             # Утилиты
│       ├── __init__.py
│       └── logger.py                        # (переиспользованный) Логгер
│
├── tests/                                 # Тесты
│   ├── core/
│   │   ├── test_event_bus.py
│   │   └── test_app_context.py
│   ├── data/
│   │   └── test_entity_graph.py
│   ├── services/
│   │   ├── test_data_service.py
│   │   └── test_connection_service.py
│   ├── projections/
│   │   ├── test_base_projection.py
│   │   └── test_tree_projection.py
│   └── ui/
│       ├── test_tree_model.py
│       ├── test_details_panel.py
│       └── test_main_window.py
│
├── requirements.txt
└── main.py                                 # Точка входа (минимальный)
```

---

### **Назначение ключевых файлов**

#### **Core Layer**
| Файл | Назначение | Откуда берётся |
|------|------------|----------------|
| `event_bus.py` | Центральная шина событий с двумя каналами | Новый |
| `constants.py` | Типы событий, режимы обновления | Новый |
| `app_context.py` | Глобальный доступ к шине и графу | Новый |

#### **Data Layer**
| Файл | Назначение | Откуда берётся |
|------|------------|----------------|
| `entity_graph.py` | Хранилище с индексами O(1) | Новый |
| `cache.py` | Кэш HTTP-ответов | Переиспользованный |

#### **Services Layer**
| Файл | Назначение | Откуда берётся |
|------|------------|----------------|
| `api_client.py` | HTTP-запросы к бэкенду | Переиспользованный (очищенный) |
| `data_service.py` | Оркестрация загрузки данных | Новый (с логикой из tree_loader) |
| `connection_service.py` | Мониторинг соединения | Новый |

#### **Projections Layer**
| Файл | Назначение | Откуда берётся |
|------|------------|----------------|
| `base_projection.py` | Базовый класс с debounce | Новый |
| `tree_projection.py` | Строит дерево из графа | Новый |

#### **UI Layer**
| Файл | Назначение | Откуда берётся |
|------|------------|----------------|
| `refresh_menu.py` | Меню выбора типа обновления | Переиспользованный |
| `tree_model.py` | Модель для QTreeView (только отображение) | Переработанный старый |
| `tree_view.py` | Визуальный компонент дерева | Переработанный старый |
| `details_panel.py` | Панель деталей (реагирует на события) | Переработанный старый |
| `main_window.py` | Композиционный корень | Переработанный старый |

#### **Models**
| Файл | Назначение | Откуда берётся |
|------|------------|----------------|
| `complex.py` | Модель комплекса | Переиспользованный |
| `building.py` | Модель корпуса | Переиспользованный |
| `floor.py` | Модель этажа | Переиспользованный |
| `room.py` | Модель помещения | Переиспользованный |

#### **Utils**
| Файл | Назначение | Откуда берётся |
|------|------------|----------------|
| `logger.py` | Профессиональное логирование | Переиспользованный |

---

### **Статистика переиспользования**

| Компонент | Строк в старом коде | Переиспользуется | Новый код |
|-----------|---------------------|------------------|-----------|
| Models | ~400 | 100% | 0 |
| Logger | ~150 | 100% | 0 |
| API Client | ~200 | 90% | ~20 |
| Cache | ~150 | 100% | 0 |
| UI Widgets (header, grid, tabs...) | ~500 | 100% | 0 |
| TreeModel | ~400 | 60% (логика отображения) | ~160 |
| TreeView | ~700 | 40% (события мыши, стили) | ~280 |
| DetailsPanel | ~500 | 70% (отображение) | ~150 |
| MainWindow | ~300 | 50% (компоновка) | ~150 |
| **ИТОГО** | **~3300** | **~70%** | **~760** |

---

### **Порядок создания (зависимости)**

```
1. core/event_bus.py
   ↑
2. data/entity_graph.py
   ↑
3. services/api_client.py
   ↑
4. services/data_service.py (зависит от event_bus, entity_graph, api_client)
5. services/connection_service.py (зависит от event_bus)
   ↑
6. projections/base_projection.py (зависит от event_bus)
7. projections/tree_projection.py (зависит от base_projection, entity_graph)
   ↑
8. ui/tree/tree_model.py (зависит от tree_projection)
9. ui/tree/tree_view.py (зависит от tree_model, event_bus)
10. ui/details/* (зависят от event_bus)
    ↑
11. ui/main_window/main_window.py (зависит от всего)
```

---

### **Критерии готовности каждого слоя**

| Слой | Критерий |
|------|----------|
| Core | Event Bus проходит тесты подписки/отписки |
| Data | Entity Graph проходит тесты индексов и связей |
| Services | DataService загружает данные и обновляет граф |
| Projections | TreeProjection строит дерево из графа |
| UI | TreeView генерирует события, DetailsPanel реагирует |
| MainWindow | Всё вместе работает по user scenarios |