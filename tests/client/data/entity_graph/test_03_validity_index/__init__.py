"""
Пакет тестов для ValidityIndex - индекса валидности данных.

ValidityIndex отвечает за:
- Пометку сущностей как валидных (mark_valid)
- Пометку сущностей как невалидных (mark_invalid)
- Проверку валидности сущностей (is_valid)
- Рекурсивную инвалидацию ветки (invalidate_branch)
- Очистку всех индексов (clear)

Каждый тест в отдельном файле для изоляции проверяемого поведения.
"""