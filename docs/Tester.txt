Вот обновленное техническое задание с учетом расположения тестера в `utils/Tester/`:

---

# Техническое Задание: Система тестирования Markoff
## Версия 1.1 (структурированная)

### Оглавление
1. [Общая концепция](#1-общая-концепция)
2. [Структура проекта](#2-структура-проекта)
3. [Компоненты тестера](#3-компоненты-тестера)
4. [Структура тестов](#4-структура-тестов)
5. [Интерфейс пользователя](#5-интерфейс-пользователя)
6. [Изоляция и воспроизводимость](#6-изоляция-и-воспроизводимость)
7. [Обработка ошибок](#7-обработка-ошибок)
8. [Критерии готовности](#8-критерии-готовности)

---

## 1. Общая концепция

### 1.1 Назначение
Создать инструмент для автоматического обнаружения и запуска тестов в проекте Markoff. Тестер реализуется как самостоятельная утилита, расположенная в `utils/Tester/`, и используется для тестирования как клиентской, так и серверной частей проекта.

### 1.2 Ключевые принципы

| Принцип | Описание |
|---------|----------|
| **Утилитарность** | Тестер — отдельный инструмент в экосистеме утилит проекта |
| **Предсказуемость** | Каждый тест изолирован, не влияет на другие |
| **Прозрачность** | Полная видимость процесса и результатов тестирования |
| **Масштабируемость** | Добавление тестов не требует изменения кода тестера |

---

## 2. Структура проекта

### 2.1 Расположение компонентов

```
Markoff_2.0/
├── run_tests.py                          # Точка входа (обертка)
├── utils/
│   ├── logger/
│   │   └── logger.py                      # Единый логгер проекта
│   └── Tester/                             # Утилита тестирования
│       ├── __init__.py
│       ├── core/                            # Ядро тестера
│       │   ├── __init__.py
│       │   ├── discovery.py                  # Обнаружение тестов
│       │   ├── runner.py                      # Запуск тестов
│       │   └── models.py                      # Структуры данных
│       ├── ui/                                # Пользовательский интерфейс
│       │   ├── __init__.py
│       │   ├── menu.py                         # Интерактивное меню
│       │   └── reporter.py                      # Формирование отчетов
│       ├── utils/                             # Внутренние утилиты
│       │   ├── __init__.py
│       │   ├── isolation.py                     # Механизмы изоляции
│       │   └── helpers.py                        # Вспомогательные функции
│       └── common/                             # Общее для тестов
│           └── test_common.py                    # База для всех тестов
│
├── tests/                                    # Тесты проекта
│   ├── __init__.py
│   ├── client/                                 # Тесты клиента
│   │   ├── __init__.py
│   │   └── core/
│   │       └── bus/
│   │           ├── __init__.py
│   │           ├── test_01_basic.py
│   │           └── test_02_subscription.py
│   └── backend/                                 # Тесты бэкенда
│       └── __init__.py
│
└── config/                                    # Конфигурация (опционально)
    └── tester_config.json
```

### 2.2 Точка входа

```python
#!/usr/bin/env python3
# run_tests.py - обертка для запуска тестера

import sys
from pathlib import Path

# Добавляем корневую директорию в путь
ROOT_DIR = Path(__file__).parent
sys.path.insert(0, str(ROOT_DIR))

from utils.Tester.ui.menu import main

if __name__ == "__main__":
    main()
```

### 2.3 Ограничения на размер

| Компонент | Максимальный размер |
|-----------|---------------------|
| Любой файл в `core/` | 300 строк |
| Любой файл в `ui/` | 400 строк |
| Любой файл в `utils/` | 200 строк |
| `common/test_common.py` | 500 строк |
| Файл с тестами | 300 строк |

---

## 3. Компоненты тестера

### 3.1 Модели данных (`core/models.py`)

```python
"""
Структуры данных для представления тестов.
Все датаклассы неизменяемые (frozen=True) для безопасности.
"""

@dataclass(frozen=True)
class TestFunction:
    """Отдельная тестовая функция"""
    name: str                    # Имя функции
    description: str             # Первая строка docstring
    module_path: str              # Путь к модулю (для импорта)
    file_path: Path               # Полный путь к файлу
    line_number: int              # Номер строки

@dataclass(frozen=True)
class TestNode:
    """Узел в дереве тестов"""
    name: str
    path: Path
    is_dir: bool
    parent: Optional['TestNode']
    children: tuple['TestNode', ...]  # Неизменяемый список
    test_functions: tuple[TestFunction, ...]
    
    def get_full_path(self) -> str:
        """Формирует путь вида client.core.bus"""
        parts = []
        current = self
        while current and current.name != "tests":
            parts.append(current.name)
            current = current.parent
        return ".".join(reversed(parts))
```

### 3.2 Обнаружение тестов (`core/discovery.py`)

```python
"""
Модуль сканирования файловой системы и загрузки тестов.
Реализует ленивую загрузку - тесты загружаются только при обращении.
"""

class TestDiscovery:
    """Основной класс для работы с деревом тестов"""
    
    def __init__(self, tests_root: Path):
        self.tests_root = tests_root
        self._root_node: Optional[TestNode] = None
        self._module_cache: Dict[Path, Any] = {}
    
    def scan(self) -> TestNode:
        """
        Рекурсивно сканирует tests_root и строит дерево.
        Алгоритм:
        1. Получить список всех элементов в директории
        2. Отфильтровать системные папки (__pycache__, скрытые)
        3. Отсортировать для стабильного порядка
        4. Для каждого элемента:
           - Если папка: создать узел и рекурсивно просканировать
           - Если файл .py (не __init__): создать узел-файл
        """
        pass
    
    def load_tests_from_file(self, node: TestNode) -> List[TestFunction]:
        """
        Загружает тестовые функции из файла.
        Использует importlib для перезагрузки модуля.
        
        Критерии тестовой функции:
        - Имя начинается с 'test_'
        - Является функцией (inspect.isfunction)
        - Определена в этом файле (не импортирована)
        """
        pass
    
    def reload(self) -> None:
        """Полная перезагрузка дерева с очисткой кэша"""
        self._module_cache.clear()
        self._root_node = self.scan()
```

### 3.3 Запуск тестов (`core/runner.py`)

```python
"""
Модуль выполнения тестов с изоляцией и сбором статистики.
"""

@dataclass
class TestResult:
    """Результат выполнения одного теста"""
    test: TestFunction
    success: bool
    duration: float
    output: str
    error: Optional[str]
    timestamp: datetime

class TestRunner:
    """Запускает тесты и собирает результаты"""
    
    def __init__(self):
        self.timeout = 10
        self.fail_fast = False
        self._reset_stats()
    
    def run_test(self, test_func: TestFunction) -> TestResult:
        """
        Запускает один тест в изолированном окружении.
        
        Последовательность:
        1. Сохранить текущее состояние
        2. Установить фиксированный seed
        3. Перенаправить stdout/stderr
        4. Запустить с таймаутом (threading)
        5. Поймать и классифицировать исключения
        6. Восстановить окружение
        7. Вернуть результат
        """
        pass
    
    def run_suite(self, tests: List[TestFunction]) -> List[TestResult]:
        """Запускает группу тестов с учетом fail-fast"""
        pass
```

### 3.4 Изоляция (`utils/isolation.py`)

```python
"""
Механизмы изоляции тестов друг от друга.
"""

def isolate_test(func: Callable, timeout: int) -> Tuple[Any, str, Optional[Exception]]:
    """
    Запускает функцию в изолированном потоке с таймаутом.
    
    Возвращает:
    - Результат функции (или None)
    - Весь перехваченный вывод
    - Исключение (или None)
    """
    pass

def reset_environment() -> None:
    """
    Полный сброс глобального состояния проекта.
    Должен обновляться при добавлении новых синглтонов.
    
    Сбрасывает:
    - EventBus (синглтон)
    - EntityGraph (глобальные индексы)
    - Логгер (категории)
    - Кэши сервисов
    """
    pass

def set_deterministic_mode(seed: int = 0) -> None:
    """Фиксирует все источники случайности"""
    random.seed(seed)
    # Также фиксируем hash seed для Python 3
    os.environ['PYTHONHASHSEED'] = str(seed)
```

### 3.5 Пользовательский интерфейс (`ui/menu.py`)

```python
"""
Интерактивное меню для управления тестированием.
"""

class TestMenu:
    """Состояние и логика пользовательского интерфейса"""
    
    def __init__(self, tests_root: Path):
        self.discovery = TestDiscovery(tests_root)
        self.runner = TestRunner()
        self.current_node = None
        self.log_level = "INFO"
        self.running = True
    
    def draw_screen(self) -> None:
        """
        Отрисовывает текущий экран.
        
        Секции:
        1. Шапка (версия, путь, статус)
        2. Текущий путь
        3. Список навигации (цифры)
        4. Список тестов в файле (буквы)
        5. Панель управления
        6. Приглашение ввода
        """
        pass
    
    def handle_input(self, key: str) -> bool:
        """
        Обрабатывает нажатие клавиши.
        
        Категории команд:
        - Навигация: 0..9 (переход/запуск)
        - Выбор теста: a..z (запуск конкретного)
        - Системные: q(выход), r(перезагрузка)
        - Режимы: f(fail-fast), t(таймаут)
        - Логи: e(ERROR), w(WARNING), i(INFO), d(DEBUG)
        
        Возвращает: False для выхода, True для продолжения
        """
        pass
```

### 3.6 Отчеты (`ui/reporter.py`)

```python
"""
Формирование отчетов о выполнении тестов.
"""

class TestReporter:
    """Генерирует читаемые отчеты"""
    
    def format_result(self, result: TestResult) -> str:
        """
        Форматирует результат одного теста.
        
        Формат:
        [статус] имя_теста (время)
          ⚠ сообщение_об_ошибке (если есть)
          📋 первые N строк вывода (опционально)
        """
        pass
    
    def format_summary(self, results: List[TestResult]) -> str:
        """
        Формирует итоговую статистику.
        
        Включает:
        - Общее количество тестов
        - Пройдено/провалено
        - Общее время
        - Детали ошибок
        - Лог выполнения
        """
        pass
```

### 3.7 База для тестов (`common/test_common.py`)

```python
"""
Единая база для всех тестов проекта.
Все тесты должны импортировать: from utils.Tester.common.test_common import *
"""

# Реэкспорт основных утилит
from utils.logger import get_logger, Logger
from utils.Tester.utils.isolation import reset_environment, set_deterministic_mode

# Базовые классы для тестов
class TestHandler:
    """Базовый обработчик событий с подсчетом"""
    
    def __init__(self, name: str):
        self.name = name
        self.events = []
        self.logger = get_logger(f"TestHandler.{name}")
    
    def handle(self, event):
        self.events.append(event)
    
    def count(self, event_type: str = None) -> int:
        if event_type:
            return sum(1 for e in self.events if e.get('type') == event_type)
        return len(self.events)
    
    def clear(self):
        self.events.clear()

# Утилиты для asserts
def assert_event_count(handler: TestHandler, expected: int, event_type: str = None):
    """Проверяет количество полученных событий"""
    actual = handler.count(event_type)
    assert actual == expected, \
        f"Ожидалось {expected} событий {event_type or 'любых'}, получено {actual}"
```

---

## 4. Структура тестов

### 4.1 Правила именования

| Объект | Формат | Пример |
|--------|--------|--------|
| Папка | `имя_компонента/` | `bus/` |
| Файл | `test_ЧЧ_имя.py` | `test_01_basic.py` |
| Функция | `test_имя` | `test_basic_subscription` |

### 4.2 Структура тестового файла

```python
"""
Модуль тестирования [название компонента].

Содержит тесты:
- test_имя1 - описание
- test_имя2 - описание
"""

from utils.Tester.common.test_common import *
from client.src.core.event_bus import EventBus

# Локальный логгер
logger = get_logger(__name__)

# Константы для тестов
TEST_EVENT = "test.event"

def test_basic_subscription():
    """
    Проверка базовой подписки и получения события.
    
    Последовательность:
    1. Создать обработчик
    2. Подписаться на событие
    3. Опубликовать событие
    4. Проверить получение
    """
    logger.info("Запуск теста базовой подписки")
    
    # Arrange
    bus = EventBus()
    handler = TestHandler("test")
    bus.subscribe(TEST_EVENT, handler.handle)
    
    # Act
    bus.emit(TEST_EVENT, {"data": 42})
    
    # Assert
    assert handler.count() == 1, "Событие не получено"
```

---

## 5. Интерфейс пользователя

### 5.1 Главный экран

```
============================================================
 🧪 MARKOFF TESTER v1.1
============================================================
 Корень: /home/user/Markoff_2.0/tests
 Уровень лога: INFO [e/w/i/d] | Fail-fast: ВЫКЛ [f] | Таймаут: 10с [t]
============================================================

📁 Текущий путь: tests
------------------------------------------------------------
 1 - 📁 client
 2 - 📁 backend

⚙️  q:выход  r:перезагрузить  e/w/i/d:лог  f:fail-fast  t:таймаут

👉 Выберите пункт:
```

### 5.2 Экран с тестами

```
📁 Текущий путь: client.core.bus
------------------------------------------------------------
 0 - 🔙 Назад / Запустить все
 1 - 📄 test_01_basic
 2 - 📄 test_02_subscription

📋 Тесты в test_01_basic:
   a - test_basic_subscription
   b - test_multiple_subscribers

👉 Выберите (0-9, a-b):
```

### 5.3 Отчет о выполнении

```
▶ Запуск тестов в client.core.bus...
------------------------------------------------------------
✅ test_basic_subscription (12мс)
✅ test_multiple_subscribers (8мс)
❌ test_weakref_cleanup (15мс)
  ⚠ AssertionError: Объект не удален

============================================================
 ИТОГ
============================================================
✅ Пройдено: 2
❌ Провалено: 1
⏱️  Время: 0.35с

❌ Детали ошибки: test_weakref_cleanup
   AssertionError: Объект не удален
   
   Вывод теста:
   12:34:56.789 DEBUG Обработчик создан
   12:34:56.790 ERROR Ссылка не очищена
```

---

## 6. Изоляция и воспроизводимость

### 6.1 Уровни изоляции

| Уровень | Механизм | Описание |
|---------|----------|----------|
| **Память** | `gc.collect()` | Принудительная сборка мусора |
| **Состояние** | `reset_environment()` | Сброс синглтонов проекта |
| **Вывод** | `StringIO` | Перехват stdout/stderr |
| **Время** | `threading` | Таймауты для зависших тестов |

### 6.2 Детерминированность

```python
# В начале каждого прогона
set_deterministic_mode(seed=0)

# Гарантирует:
# - Одинаковый порядок в random.shuffle
# - Одинаковые hash'ы
# - Воспроизводимость ошибок
```

---

## 7. Обработка ошибок

### 7.1 Классификация и реакция

| Тип ошибки | Пример | Реакция |
|------------|--------|---------|
| **AssertionError** | `assert x == y` | Провал теста, сбор контекста |
| **TimeoutError** | Тест выполняется >10с | Принудительное прерывание |
| **ImportError** | Модуль не найден | Пропуск файла, предупреждение |
| **Exception** | Непредвиденная ошибка | Полный traceback |

### 7.2 Graceful Shutdown

```python
def setup_signal_handlers():
    """Обработка Ctrl+C и других сигналов"""
    
    def handler(signum, frame):
        print("\n\n⚠️  Получен сигнал прерывания")
        print("Завершаем текущий тест...")
        # Сохраняем частичные результаты
        # Чистим ресурсы
        sys.exit(0)
    
    signal.signal(signal.SIGINT, handler)
```

---

## 8. Критерии готовности

### 8.1 Функциональные требования

- [ ] Обнаружение всех тестов в `tests/` (рекурсивно)
- [ ] Навигация по папкам (цифры 1..N)
- [ ] Запуск всех тестов в папке (0)
- [ ] Запуск отдельных тестов (a..z)
- [ ] Переключение уровней логирования (e/w/i/d)
- [ ] Перезагрузка дерева тестов (r)
- [ ] Режим fail-fast (f)
- [ ] Настройка таймаута (t)

### 8.2 Технические требования

- [ ] Все компоненты в `utils/Tester/`
- [ ] Каждый файл не превышает лимит строк
- [ ] Используется единый логгер из `utils/logger/`
- [ ] Корректная перезагрузка модулей
- [ ] Таймауты работают на Windows
- [ ] Фиксированный seed для воспроизводимости
- [ ] Обработка Ctrl+C

### 8.3 Требования к тестам

- [ ] Каждый тест использует `test_common`
- [ ] Каждый тест имеет docstring
- [ ] Нет зависимостей между тестами
- [ ] Тесты не оставляют мусора

---

## Приложение: Быстрый старт

```bash
# Запуск
python run_tests.py

# Навигация
1 → client
1 → core
1 → bus

# Запуск всех в папке
0

# Запуск конкретного теста
a

# Выход
q
```

---

*Документ версии 1.1 от 2026-03-09*




Шаблон тестового файла
"""
Модуль тестирования [название компонента].

Содержит тесты:
- test_basic_behavior - базовая проверка работы
- test_error_handling - проверка обработки ошибок
- test_edge_case - проверка граничного случая
"""

# ===== Imports =====

from utils.Tester.common.test_common import *

# Импорт тестируемого компонента
from client.src.core.event_bus import EventBus

# ===== Логгер =====

logger = get_logger(__name__)

# ===== Константы =====

TEST_EVENT = "test.event"
TEST_DATA = {"value": 42}


# ==========================================================
# БАЗОВЫЕ ТЕСТЫ
# ==========================================================

def test_basic_behavior():
    """
    Проверка базовой функциональности компонента.
    
    Последовательность:
    1. Создать экземпляр
    2. Выполнить базовую операцию
    3. Проверить результат
    """

    logger.info("Запуск test_basic_behavior")

    # ---------- Arrange ----------
    component = EventBus()

    # ---------- Act ----------
    result = component is not None

    # ---------- Assert ----------
    assert result, "Компонент не создан"


# ==========================================================
# ФУНКЦИОНАЛЬНЫЕ ТЕСТЫ
# ==========================================================

def test_basic_subscription():
    """
    Проверка подписки на событие.

    Последовательность:
    1. Создать шину
    2. Создать обработчик
    3. Подписаться на событие
    4. Опубликовать событие
    5. Проверить получение
    """

    logger.info("Запуск test_basic_subscription")

    # ---------- Arrange ----------
    bus = EventBus()
    handler = TestHandler("handler")

    bus.subscribe(TEST_EVENT, handler.handle)

    # ---------- Act ----------
    bus.emit(TEST_EVENT, TEST_DATA)

    # ---------- Assert ----------
    assert handler.count() == 1, "Обработчик не получил событие"


# ==========================================================
# ТЕСТЫ ОБРАБОТКИ ОШИБОК
# ==========================================================

def test_error_handling():
    """
    Проверка поведения при некорректных данных.

    Последовательность:
    1. Создать компонент
    2. Передать некорректные данные
    3. Проверить корректную обработку
    """

    logger.info("Запуск test_error_handling")

    # ---------- Arrange ----------
    bus = EventBus()

    # ---------- Act ----------
    error = None
    try:
        bus.emit(None, {})
    except Exception as e:
        error = e

    # ---------- Assert ----------
    assert error is not None, "Ошибка не возникла"


# ==========================================================
# ГРАНИЧНЫЕ СЛУЧАИ
# ==========================================================

def test_edge_case():
    """
    Проверка граничного случая.

    Последовательность:
    1. Создать компонент
    2. Выполнить операцию с граничными параметрами
    3. Проверить результат
    """

    logger.info("Запуск test_edge_case")

    # ---------- Arrange ----------
    bus = EventBus()

    # ---------- Act ----------
    result = bus.emit("unknown.event", {})

    # ---------- Assert ----------
    assert result is None or result == 0



Ниже аккуратный каталог тестов, который покрывает практически все режимы работы тестера.

Тесты обнаружения тестов (Discovery)
test_discovery_single_function
Проверяет, что тестер обнаруживает один тест в файле.
test_discovery_multiple_functions
Проверяет корректное обнаружение нескольких тестов в одном файле.
test_discovery_ignore_non_tests
Проверяет, что функции без префикса test_ не считаются тестами.
test_discovery_mixed_file
Проверяет корректное обнаружение тестов в файле, где присутствуют и тестовые, и обычные функции.
test_discovery_class_methods
Проверяет обнаружение тестовых методов внутри классов.
test_discovery_ignore_private_functions
Проверяет, что приватные функции (_name) не считаются тестами.
test_discovery_empty_file
Проверяет поведение тестера при загрузке файла без тестов.
test_discovery_syntax_error_file
Проверяет корректную обработку файла с синтаксической ошибкой.

Тесты выполнения тестов
test_execution_single_pass
Проверяет корректный запуск одного успешного теста.
test_execution_multiple_pass
Проверяет запуск нескольких успешных тестов подряд.
test_execution_mixed_results
Проверяет корректную обработку смеси успешных и падающих тестов.
test_execution_order
Проверяет, что тесты выполняются в ожидаемом порядке.

Тесты обработки ошибок
test_assertion_failure
Проверяет корректную обработку AssertionError.
test_runtime_exception
Проверяет обработку обычного исключения во время выполнения теста.
test_traceback_capture
Проверяет, что traceback ошибки корректно сохраняется в результатах.
test_error_does_not_stop_runner
Проверяет, что падение одного теста не останавливает выполнение остальных.

Тесты вывода (stdout / stderr)
test_stdout_capture
Проверяет перехват вывода stdout внутри теста.
test_stderr_capture
Проверяет перехват вывода stderr.
test_multiple_output_lines
Проверяет корректный захват многострочного вывода.

Тесты измерения времени
test_duration_recorded
Проверяет, что тестер записывает время выполнения теста.
test_duration_accuracy
Проверяет приблизительную точность измерения времени.
test_slow_test_handling
Проверяет корректную обработку длительно выполняющихся тестов.

Тесты управления выполнением
test_shutdown_interrupt
Проверяет корректную остановку выполнения тестов при сигнале остановки.
test_partial_execution_after_interrupt
Проверяет, что после сигнала остановки выполняется только часть тестов.
Тесты стабильности тестера
test_keyboard_interrupt_handling
Проверяет корректную обработку KeyboardInterrupt.
test_system_exit_handling
Проверяет корректную обработку SystemExit.
test_memory_error_handling
Проверяет устойчивость тестера к MemoryError.
test_unexpected_exception_handling
Проверяет обработку неожиданных исключений.

Тесты структуры результатов
test_session_creation
Проверяет создание сессии тестирования.
test_result_collection
Проверяет сбор результатов тестов в сессию.
test_result_statistics
Проверяет корректный подсчет количества успешных и проваленных тестов.
test_session_duration
Проверяет корректный расчет общего времени тестовой сессии.

Тесты системы отчетов
test_result_formatting
Проверяет форматирование вывода одного результата теста.
test_session_report_formatting
Проверяет форматирование итогового отчета тестовой сессии.
test_progress_display
Проверяет корректность отображения прогресса выполнения тестов.

Тесты навигации UI
test_pagination_creation
Проверяет создание пагинированного списка.
test_pagination_next_page
Проверяет переход на следующую страницу.
test_pagination_previous_page
Проверяет переход на предыдущую страницу.
test_pagination_bounds
Проверяет корректную обработку выхода за границы страниц.

Тесты загрузки тестовых файлов
test_file_loading_success
Проверяет успешную загрузку тестового файла.
test_file_loading_error
Проверяет обработку ошибки загрузки файла.
test_file_cache_behavior
Проверяет работу кеширования тестовых файлов.

Стресс-тесты
test_large_test_suite_discovery
Проверяет обнаружение большого количества тестов.
test_large_test_suite_execution
Проверяет запуск большого количества тестов.
test_runner_memory_stability
Проверяет стабильность использования памяти при большом числе тестов.

Мета-тесты (самопроверка тестера)
test_pass_fail_error_classification
Проверяет корректную классификацию PASS / FAIL / ERROR.
test_output_integrity
Проверяет целостность собранных результатов.
test_report_consistency
Проверяет согласованность итогового отчета с результатами тестов.