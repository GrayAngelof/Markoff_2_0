"""
Тест 8: Файл с синтаксической ошибкой.

Проверяет, что тестер корректно обрабатывает файлы с синтаксическими ошибками
и не падает, а сообщает об ошибке.
"""

# Этот файл содержит синтаксическую ошибку!
# Раскомментируйте следующую строку, чтобы активировать тест:

def test_with_syntax_error(:
    assert True

# Правильный тест для проверки обработки ошибок
from utils.Tester.common.test_common import test


@test("Проверка обработки синтаксических ошибок")
def test_syntax_error_handling():
    """
    Этот тест проверяет, что тестер корректно сообщает
    о файлах с синтаксическими ошибками.
    """
    # Здесь мы не можем напрямую проверить синтаксическую ошибку,
    # так как это тест, а не раннер. Но мы можем проверить,
    # что механизм обработки ошибок существует.
    
    import sys
    from pathlib import Path
    
    # Проверяем, что discovery может загрузить этот файл
    # (даже с ошибкой, он должен вернуть пустой результат)
    from utils.Tester.core.discovery import TestDiscovery
    
    discovery = TestDiscovery(Path(__file__).parent.parent)
    
    # Должно работать без падений
    assert True